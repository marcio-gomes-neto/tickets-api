{
  "name": "global-database",
  "private": true,
  "version": "1.1.0",
  "description": "Global database package",
  "main": "./build/index.js",
  "scripts": {
    "ts-build": "tsc",
    "ts-watch": "tsc --watch",
    "lint": "eslint src/**",
    "lint-fix": "esw src/** --fix",
    "lint-watch": "cls && esw --watch src/**",
    "build": "del-cli --force ./build && tsc",
    "clean": "del-cli --force ./build"
  },
  "author": "Márcio Gomes",
  "license": "ISC",
  "dependencies": {
    "del-cli": "^5.0.0"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
